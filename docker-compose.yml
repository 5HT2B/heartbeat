version: "3.9"
services:
  server:
    image: l1ving/heartbeat
    volumes:
      - "$HB_PATH:/heartbeat/config"
    ports:
      - "6060:6060"
    build:
      context: .

  database:
    image: redislabs/rejson:latest
    expose:
      - 6379
    ports:
      - "6379:6379"

